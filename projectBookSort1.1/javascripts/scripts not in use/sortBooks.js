// As a user, it should be possible to re-organize the books according to 
// the selected option, in alphabetical order

// 


/*

const sortBy = function(){
    let books = [];
    books.sort();

}


sort.addEventListener('change', sortBy);

const sortByTitle = function(selected){
    let tempArr = new Array();
    for (let i = 0; i < selected.options.length; i++){
        tempArr[i] = new Array();
        tempArr[i][0] = selected.options[i].value;
    }
    tempArr.sort();

}
const sortBy = function (){
    let title = $('bookTitle');
    let author = $('bookAuthor');
        if(title){
            let tempArr = new Array();
            tempArr.push(books)

        }
        if(author){

        }
        else{

        }
    }

function addActivityItem(){
    //option is selected
    alert("yeah");
}
sort.addEventListener("change", sortBy, false);
*/